[databases]
smc_trading_agent = host=postgresql-primary port=5432 dbname=smc_trading_agent
smc_trading_agent_read = host=postgresql-replica port=5432 dbname=smc_trading_agent

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = md5
auth_file = /etc/pgbouncer/userlist.txt
admin_users = postgres
stats_users = postgres

# Connection pooling settings
pool_mode = transaction
max_client_conn = 1000
default_pool_size = 25
min_pool_size = 5
reserve_pool_size = 5
reserve_pool_timeout = 5

# Connection limits per database
max_db_connections = 100
max_user_connections = 100

# Timeouts
server_reset_query = DISCARD ALL
server_check_delay = 30
server_check_query = SELECT 1
server_lifetime = 3600
server_idle_timeout = 600
client_idle_timeout = 0

# Logging
log_connections = 1
log_disconnections = 1
log_pooler_errors = 1
verbose = 0

# Performance tuning
tcp_keepalive = 1
tcp_keepcnt = 3
tcp_keepidle = 600
tcp_keepintvl = 30

# Security
ignore_startup_parameters = extra_float_digits

# Application-specific pools
[databases]
# Main application pool - optimized for OLTP workloads
smc_trading_agent = host=postgresql-primary port=5432 dbname=smc_trading_agent pool_size=20 max_db_connections=50

# Read-only queries pool - for analytics and reporting
smc_trading_agent_read = host=postgresql-replica port=5432 dbname=smc_trading_agent pool_size=15 max_db_connections=30

# High-frequency trading data pool - smaller transactions
smc_trading_hft = host=postgresql-primary port=5432 dbname=smc_trading_agent pool_size=30 max_db_connections=60

# Background jobs pool - for data processing
smc_trading_jobs = host=postgresql-primary port=5432 dbname=smc_trading_agent pool_size=10 max_db_connections=20